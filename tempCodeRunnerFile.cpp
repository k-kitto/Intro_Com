#include <iostream>
#include <algoritm>
#include <string>
using namespace std ;

int main () {
    int n[3] ;
    for ( i=0 ; i<3 ; i++ ) {
        cin >> n[i] ;
    }

    string str ;
    cin >> str ;

    sort(n.begin(), n.end()) ;

    for( int i=0 ; i<3 ; i++ ) {
        cout << a[str-A] << " " ;
    }

    return 0 ;
}